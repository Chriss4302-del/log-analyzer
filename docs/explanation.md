# Explanation

This script goes through each line in a log file and checks for specific keywords.

- "Failed password" is used to detect failed login attempts
- "Accepted" is used to detect successful logins

The script counts these events and prints a summary.

This is a simple example, but it shows how logs can be analyzed to understand system activity.
